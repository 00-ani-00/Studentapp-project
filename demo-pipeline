pipeline {
    agent any

    stages {
        stage('Create File') {
            steps {
                sh 'mkdir aniket'
                sh 'touch aniket/example{1..10}.txt'
            }
        }
    }
}
